# AI 调试功能增强测试

## 🎯 功能概述

已完成对 AI 调试功能的重大升级，主要包括：

### 1. 📋 限制支持的模型
- **OpenAI**: 仅支持 GPT-4o, GPT-4, GPT-5-mini（用于调试）
- **DashScope**: 仅支持 qwen-plus（用于调试）
- **移除不支持的提供商**: 自动过滤出不支持调试的提供商和模型

### 2. 🛠️ 增强的参数管理
- **格式化类型支持**: 
  - 方括号格式：`[参数名]`
  - 花括号格式：`{参数名}`
  - 无格式化：手动管理参数
- **自动参数提取**: 根据选择的格式化类型自动识别模板中的参数
- **手动参数管理**: 
  - 添加自定义参数
  - 删除不需要的参数
  - 重命名参数

### 3. 🎨 改进的用户界面
- **参数分类显示**: 区分自动检测和手动添加的参数
- **智能验证**: 检查模型是否支持调试，参数是否完整
- **友好提示**: 为用户提供操作指导

## 🧪 测试用例

### 测试用例 1: 模型限制验证
**步骤**:
1. 打开 AI 调试页面
2. 查看 AI 提供商下拉选项
3. 选择不同提供商，查看模型选项

**预期结果**:
- 只显示 OpenAI 和 DashScope 两个提供商
- OpenAI 只显示 GPT-4o, GPT-4, GPT-5-mini
- DashScope 只显示 qwen-plus

### 测试用例 2: 方括号格式参数提取
**步骤**:
1. 选择参数格式为"方括号 []"
2. 输入模板：`你好 [用户名]，欢迎使用 [产品名称]！`
3. 查看参数列表

**预期结果**:
- 自动检测到"用户名"和"产品名称"两个参数
- 提供输入框让用户填写参数值

### 测试用例 3: 花括号格式参数提取
**步骤**:
1. 选择参数格式为"花括号 {}"
2. 输入模板：`请为 {公司名} 写一份关于 {主题} 的报告`
3. 查看参数列表

**预期结果**:
- 自动检测到"公司名"和"主题"两个参数
- 正确显示在参数列表中

### 测试用例 4: 手动参数管理
**步骤**:
1. 选择参数格式为"无"
2. 点击"添加参数"按钮
3. 输入参数名称和值
4. 测试删除参数功能

**预期结果**:
- 成功添加自定义参数
- 参数名称可以编辑
- 删除功能正常工作

### 测试用例 5: 参数替换验证
**步骤**:
1. 设置模板：`[用户名] 想了解 [产品] 的功能`
2. 填写参数：用户名="张三"，产品="AI助手"
3. 查看最终 prompt

**预期结果**:
- 最终 prompt 显示：`张三 想了解 AI助手 的功能`
- 参数正确替换

### 测试用例 6: 调试功能验证
**步骤**:
1. 完整配置所有必需字段
2. 点击"开始调试"
3. 验证 AI 响应

**预期结果**:
- 只有支持的模型才能进行调试
- 参数未填写完整时按钮禁用
- 调试过程正常运行

## 🔧 技术实现

### 新增配置项
- `DEBUG_SUPPORTED_MODELS`: 定义支持调试的模型
- `getDebugSupportedProviders()`: 获取支持调试的提供商
- `getDebugSupportedModelsByProvider()`: 获取指定提供商的调试支持模型
- `isModelSupportedForDebug()`: 检查模型是否支持调试

### 增强的组件状态
- `formatType`: 参数格式化类型
- `customParameters`: 自定义参数列表
- 改进的参数提取逻辑（支持多种格式）
- 智能的参数验证和模型检查

### 用户体验改进
- 参数分类显示（自动vs手动）
- 实时参数验证
- 智能提示和错误处理
- 响应式布局和样式优化

## 🚀 部署建议

1. **测试所有格式化类型**: 确保方括号、花括号、无格式化都能正常工作
2. **验证模型限制**: 确认只有支持的模型出现在选项中
3. **测试参数管理**: 验证添加、删除、重命名参数的所有功能
4. **调试流程测试**: 完整测试从配置到结果输出的整个流程
5. **错误处理**: 测试各种边界情况和错误场景

## 📝 使用说明

### 基本用法
1. 选择支持调试的 AI 提供商和模型
2. 选择参数格式化类型
3. 输入 prompt 模板
4. 填写参数值（自动检测 + 手动添加）
5. 开始调试并查看结果

### 高级功能
- **混合参数**: 支持自动检测和手动添加的参数混合使用
- **参数重用**: 同一个参数可以在模板中多次使用
- **动态调整**: 可以随时修改参数值进行多轮调试
- **格式切换**: 支持动态切换参数格式化类型

这个增强版本的 AI 调试功能提供了更强大和灵活的参数管理能力，同时确保只使用经过验证的、支持调试的 AI 模型。